package point

import "math"

const (
	// RadToDeg = Radians to Degrees
	RadToDeg = 180 / math.Pi
	// DegToRad = Degrees to Radians
	DegToRad = math.Pi / 180
	// RadToGrad = Radians to Grade
	RadToGrad = 200 / math.Pi
	// GradToDeg = Grade to Degrees
	GradToDeg = math.Pi / 200
)
